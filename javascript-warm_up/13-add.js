#!/usr/bin/node

// Define and export a function named `add`
// The function takes two parameters, `a` and `b`, which are integers
// It returns the sum of `a` and `b`
module.exports.add = function add(a, b) {
  return a + b;
};
