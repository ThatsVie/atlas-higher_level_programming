#!/usr/bin/python3

def update_dictionary(a_dictionary, key, value):
    # Update dictionary with new key-value pair
    # or modify the value of an existing key
    a_dictionary[key] = value
    return a_dictionary
